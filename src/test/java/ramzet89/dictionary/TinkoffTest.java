package ramzet89.dictionary;

public class TinkoffTest {
    int startIndex = 0;

      for (int i=0; i< A.length; i++) {
        if (Math.abs(A[i])<Math.abs(A[startIndex])) {
            startIndex = i;
        }
    }
}
